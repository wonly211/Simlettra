# 澄笺 | Simlettra 智能体协作规范

## 项目目标

在本仓库中重构「澄笺 | Simlettra」。重构不是逐文件翻译或照搬旧项目，而是在理解旧系统的基础上，重新确认产品需求、目标架构、技术路线、数据迁移和交付计划。

当前项目已经完成需求基线及十一项需求变更，接受第一批至第七批架构和第一批至第六批完整数据模型，并已进入纵向切片开发。正式工程现已包含单 Worker 四入口、Vue 网页、Hono HTTP 边界、系统状态、三步网页初始化向导、密码登录、可撤销会话、密码修改、管理员临时密码重置、`init_key` 管理员恢复、普通用户创建与状态管理、账号邀请码注册、唯一管理员转让、用户注销与七天恢复、用户和组织期满清理、邮件域名管理、个人主地址与别名管理、全局地址策略与别名保留期、组织身份与成员协作、邮件接收与完整性提交、统一收件箱、安全读信、邮箱整理、垃圾箱、邮件永久删除、邮箱组合搜索、邮件会话归并、草稿与写信编辑、正式发信、回复、回复全部、转发、外部通知网关、外部邮箱验证与个人来信自动转发、管理员免费资源用量与保守停止、用户和组织邮件导出、管理员本地备份与空白资源完整恢复、旧系统数据迁移、网页新邮件自动更新，以及管理员运行健康状态。首发域外发信只支持 Resend 和 SMTP2GO，不支持 Cloudflare Email Sending。Simlettra 信任唯一系统管理员添加的邮件域名，不重复验证域名所有权。邮箱地址采用 ASCII 前缀、统一小写和 IDNA/Punycode 域名规范化。初始化管理员和新建普通用户默认拥有 20 个个人别名额度并允许在额度内自行创建，同时默认最多创建 5 个组织。D1、KV 和 R2 的 Cloudflare 免费额度是产品资源上限，Workers Free 是首发核心运行基线；20 MB 邮件解析必须先通过真实 Workers Free Queue consumer 验证，验证失败时重新提交产品方案，不得静默降低需求或改用 Paid。

## 仓库边界

- `E:\SynologyDrive\github\Simlettra` 是新项目仓库，也是唯一默认写入目标。
- `E:\SynologyDrive\github\Simletter` 是旧系统参考仓库，默认只读。
- 旧系统参考提交为 `9d016831ff2d862e38c08d9376a58327bc8933df`。
- 未经用户明确要求，不得修改、格式化、安装依赖、生成产物或提交旧仓库中的文件。
- 旧代码和旧测试用于提供事实证据，不自动成为新项目需求或架构约束。

## 事实来源优先级

发生冲突时，按以下顺序处理：

1. 用户在当前任务中的明确要求。
2. 本文件中的仓库级协作规范。
3. 状态为“已接受”的架构决策记录。
4. 标记为“当前”的产品与架构文档。
5. 新项目中已经通过测试验证的代码行为。
6. 旧项目的代码、测试与文档。

不得把推测、建议、旧系统行为或“提议中”的架构决定写成已确认事实。

## 小型软件开发流程

项目采用轻量但完整的开发流程，不为流程而流程，也不跳过关键决策。

### 阶段一：需求确认

1. 明确产品目的、主要用户和核心使用场景。
2. 按功能模块批量确认需求，减少逐条往返。
3. 为每项需求标记“待确认”“已确认”“已取消”或“已取代”。
4. 区分首个可用版本、后续版本和明确不做的范围。
5. 用户决定产品行为；智能体负责解释影响、发现冲突并给出推荐。

在需求未确认前，不得用技术实现反向替用户决定产品行为。

### 阶段二：架构与技术路线

1. 根据已确认需求整理质量属性和约束。
2. 提出少量可比较的架构与技术方案，说明成本、风险和取舍。
3. 对长期影响部署、数据、模块边界、安全或迁移的决定建立架构决策记录。
4. 目标架构和核心技术路线获得接受后，才建立正式工程骨架。
5. 验证性原型必须明确标记，不得被误认为生产实现。

### 阶段三：迭代开发

1. 将工作拆成可演示、可测试、可回滚的纵向功能切片。
2. 每个切片先定义验收条件，再实现代码。
3. 优先完成最小闭环，不提前建设没有真实用例支撑的通用框架。
4. 每次变更保持范围清晰，不顺手重构无关模块。
5. 完成实现、测试、文档和必要迁移后，才视为切片完成。

### 阶段四：发布与维护

1. 发布前执行自动化检查、数据迁移验证和回滚演练。
2. 版本说明必须列出行为变化、数据影响、配置变化和已知限制。
3. 生产问题优先建立可复现证据和回归测试，再修复实现。
4. 重要故障结束后记录原因、影响、修复和预防措施。

## 单项任务工作方式

处理任何代码或文档任务时，按以下顺序工作：

1. **理解**：明确目标、非目标、验收条件和风险。
2. **探索**：读取相关文件、测试、数据结构和历史实现。
3. **判断**：区分已确认事实、合理推断和仍需用户决定的事项。
4. **计划**：为较大任务列出可检查的步骤，并持续更新状态。
5. **实现**：遵守现有边界，以最小完整改动完成目标。
6. **验证**：运行与风险相称的测试、类型检查、构建或人工验证。
7. **记录**：同步更新需求、架构决策、迁移说明和操作文档。
8. **交付**：说明完成内容、验证结果、剩余风险和下一步。

发现阻塞时，应先自行调查和缩小问题范围。只有在无法安全推断且会影响产品行为、数据安全或破坏性操作时，才请求用户决定。

## 需求管理规范

- 需求使用简洁、可观察的用户行为描述，不使用实现细节代替需求。
- 每个模块提供一张批量确认表，包含编号、建议方案和状态。
- 用户可以回复“本模块全部按推荐”，或只列出需要修改的编号。
- 已确认需求发生变化时，必须同步更新所有当前文档，并明确旧结论已被取代。
- 未确认项不得进入开发承诺、验收标准或已接受架构决策。
- 不使用旧需求作为默认值。每次需求重置后，从新的当前文档重新读取事实。

## 架构决策规范

- 架构决策记录存放于 `文档/架构决策记录/`。
- 文件命名格式为 `NNNN-简体中文标题.md`，编号递增且不复用。
- 状态只使用“提议中”“已接受”“已取代”“已拒绝”。
- 决策记录至少包含背景、决定、备选方案、后果和验证方式。
- 已接受的决定不直接改写结论；使用新记录取代，并建立双向链接。
- 改变部署单元、模块所有权、数据存储、认证授权、公共契约或迁移方式时，必须先记录决策。

## 文档语言与命名

- 所有项目文档必须使用简体中文，包括标题、正文、表格、图示文字、注释性说明和面向用户的元数据。
- 文档目录名和可自由命名的文档文件名必须使用简体中文，不得新建以英文为主的文档目录或文件名。
- `README.md`、`AGENTS.md`、`SKILL.md`、配置文件名和其他由工具或行业规范强制识别的名称可以保留约定写法，但文件内容仍必须使用简体中文。
- 技术名称、代码标识、命令、协议字段和第三方产品名保留准确原文，解释使用简体中文。
- 引用外文资料时优先使用简体中文概述；必须保留的短句原文应同时提供简体中文说明。
- 文档中的事实、推断、建议和待确认事项必须明确区分。
- 修改文档后检查标题、状态、内部链接、文件名和日期。

## 代码与依赖原则

- 在技术路线确定后，遵循仓库已经采用的语言、框架、格式化和目录模式。
- 优先使用成熟标准库和项目已有工具，不重复实现可靠的基础能力。
- 新增依赖前说明用途、维护状态、运行成本、许可证和替代方案。
- 不为单一用例提前创建通用仓储、全局服务、事件总线或工具包。
- 跨模块协作必须通过明确公开接口，不得直接访问其他模块的内部存储实现。
- 数据库结构通过版本化迁移演进，不在普通请求中隐式建表或破坏性修改结构。
- 多资源写入必须明确事务、幂等、补偿、恢复或重放策略。
- 不在全局可变单例中保存请求级用户、权限、配置或资源绑定。

## 测试与质量门槛

- 测试范围与风险和影响面相称。
- 纯业务规则优先使用快速单元测试。
- 数据查询、迁移和存储适配器使用真实或高保真集成测试。
- HTTP、邮件接收、定时任务和外部适配器需要边界集成测试。
- 登录、收信、读信、写信、删除和管理配置等关键流程需要浏览器端到端测试。
- 从旧系统迁移的行为先建立特征测试，再替换实现。
- 缺陷修复必须尽可能添加能够复现问题的回归测试。
- 没有测试、可执行验证或明确证据时，不得声称与旧系统完全一致。

当前正式工程统一使用以下命令：

- `pnpm dev`：启动本地 Vue 网页和 Worker 开发环境。
- `pnpm db:migrate:local`：应用尚未执行的本地正式 D1 迁移。
- `pnpm db:migrate:remote`：在完成备份、影响确认和回退准备后，通过 D1 文件导入通道逐个应用远程正式迁移，并沿用 `d1_migrations` 唯一账本。
- `pnpm format`：格式化当前正式工程代码与配置。
- `pnpm lint`：检查 TypeScript 与 Vue 代码规则。
- `pnpm typecheck`：生成 Cloudflare 类型并检查网页、Worker、配置与测试类型。
- `pnpm test`：在 Cloudflare Workers Vitest pool 中执行单元和边界集成测试。
- `pnpm test:backup-restore`：在临时本地 D1 与 KV/R2 中执行备份恢复完整自检。
- `pnpm test:legacy-migration`：在临时本地 D1 与 KV/R2 中执行旧系统数据迁移完整自检。
- `pnpm test:upgrade-rollback`：从上一正式迁移重建代表性数据，验证当前迁移、数据检查和独立回退目标。
- `pnpm test:early-storage-migration`：验证已有用量账本和缺少用量账本两种早期逻辑配额形状均可安全升级。
- `pnpm test:security`：聚合越权、脚本内容边界、恶意附件名、令牌泄露、重复投递与重复发送回归，并执行生产密钥扫描。
- `pnpm test:accessibility`：使用 Vue 模板 AST 检查原生交互控件、可访问名称、表单标签、按钮类型和键盘顺序风险。
- `pnpm test:deployment-config`：分别安全构建 R2 与 KV 模板产物，检查绑定、存储模式、Queue、Cron、网页回退和本机路径泄露。
- `pnpm test:performance`：应用全部正式迁移，以 50 名用户、100,000 封邮件和 10 个独立读取线程测量列表与详情数据查询。
- `pnpm build`：生成单 Worker 生产构建和网页资源，检查产物不含本地密钥文件、已知密钥内容或生成配置中的本机绝对路径。
- `pnpm package:source`：按白名单生成开发版源码 ZIP、逐文件清单和 ZIP SHA-256，排除本地密钥、依赖、构建产物、日志、临时数据库及大样本；产物不自动成为正式发布版本。
- `pnpm check`：依次执行格式、代码规则、类型、测试、备份恢复自检、旧系统迁移自检、升级回退自检、早期配额迁移自检、性能基准、网页控件可访问性、双模式部署配置自检和构建，是提交前统一质量门槛。

## 安全与数据保护

- 邮件正文、附件、账号、令牌和用户设置均视为敏感数据。
- 日志不得记录密码、密钥、会话令牌、完整正文或附件内容。
- 所有资源访问必须在服务端执行所有权或授权检查，不能依赖前端隐藏。
- 认证信息不得放入 URL、公开日志或不受保护的长期前端存储。
- 接收与发送邮件必须考虑重复投递、重试、部分失败和恶意输入。
- HTML 邮件、附件名、外部链接和远程图片必须按不可信输入处理。
- 删除、格式化、迁移和批量清理必须提供明确确认、影响说明和恢复或回滚方案。
- 不提交真实用户数据、生产邮件样本、Cloudflare 资源标识或密钥。
- 每套系统的唯一系统管理员是可信的安全责任人，可以查看和管理 `INIT_KEY`、`CONFIG_KEY` 及网页中的动态服务凭据；不得以防范该管理员为理由增加隐藏、一次性展示或本机二次封装。密钥仍不得进入 GitHub、构建日志或普通运行日志。

## 旧系统迁移原则

- 旧仓库默认只读，先定位入口、数据写入、权限检查、失败行为和现有测试。
- 保留旧行为必须有产品需求或迁移兼容性依据，不能仅因为旧代码已经存在。
- 迁移工具与正常运行路径分离，破坏性转换必须显式确认。
- 数据迁移需要提供数量对账、对象引用检查、失败报告和可重复执行策略。
- 新旧系统对比必须记录参考提交、测试环境和未覆盖差异。

## Git 与变更纪律

- 初始化 Git 后，主分支应始终保持可构建、可测试或明确标注尚未具备工程骨架。
- 一个提交只表达一个完整目的，提交说明使用简体中文并描述行为变化。
- 不提交依赖目录、构建产物、临时文件、真实配置、密钥或用户数据。
- 不覆盖、回滚或清理用户未授权的现有修改。
- 禁止使用破坏性 Git 命令，除非用户明确要求并已说明影响。
- 未经用户明确要求，不自动提交、推送、创建标签或发布版本。

## 完成定义

一项工作只有同时满足以下条件才算完成：

- 验收条件已经满足。
- 必要实现与配置已经落盘。
- 相关测试、检查或构建已经通过。
- 数据、安全、权限和迁移风险已经处理或明确记录。
- 当前需求、架构决策和操作文档已经同步。
- 没有遗留运行中的必要命令或未说明的失败。
- 交付说明准确列出完成内容、验证结果和剩余事项。

## 当前项目状态

- 需求变更 `0009` 已接受并生效：`D1 + KV` 模式下用户与组织默认逻辑存储配额各为 `100,000,000` 字节，`D1 + R2` 模式下各为 `1,000,000,000` 字节；系统管理员可以调整默认值和单主体覆盖，调低额度不删除数据，只阻止继续增长。
- 第二十二批用户与组织逻辑存储配额纵向切片已经完成，正式迁移为 `0016-用户与组织逻辑存储配额.sql`。
- 第二十三批用户与组织邮件导出纵向切片已经完成本地闭环，正式迁移为 `0017-用户与组织邮件导出.sql`；个人邮件由本人导出，组织邮件由当前组织创建者导出，导出范围在创建时冻结，结果按约 20 MB 源邮件字节拆成 ZIP 分卷。
- 导出使用现有 `background_tasks` 逐卷推进，每次最多生成一个分卷，D1 项目状态作为检查点；Queue 发送失败不影响创建结果，Cron 可依据任务账本补投。导出制品默认保留七天，不计入用户与组织逻辑配额但计入 KV/R2 平台容量。
- 第二十四批管理员本地备份与完整恢复纵向切片已经完成本地闭环，正式迁移为 `0018-管理员备份与恢复.sql`；管理员在线生成 D1 表和当前 KV/R2 对象的可校验原始分卷，本地 `pack` 才生成默认 AES-256-GCM 加密容器。
- 恢复首发只允许迁移版本一致的空白 D1 与空白 KV/R2 目标；D1 按正式迁移外键拓扑导入，可空自引用先置空后回填，对象上传后逐项回读校验。清单哈希、表行数、对象哈希、外键、对象引用和搜索重建六项全部通过后才能成功。
- D1 外部 SQL 和跨 D1、对象、搜索的恢复不具备整次原子事务；任一步失败后必须废弃目标资源并从空白目标重新执行。临时本地 `D1 + KV` 与 `D1 + R2` 完整恢复自检、备份接口 `2/2` 和导出及资源测试 `13/13` 已通过，真实 Cloudflare Free 远程 D1、KV、R2 恢复仍需演练。
- 第二十五批旧系统数据迁移纵向切片已经完成本地闭环，正式迁移为 `0019-旧系统数据迁移.sql`；本地工具只读提取旧 D1 与被引用的 KV/R2 对象，建立稳定快照，并通过独立演练报告约束正式迁移。
- 迁移使用确定性编号、不可变来源映射、对象写入回读、D1 原子可见提交、检查点和七类对账；旧弱密码不迁移，旧结构化邮件明确标记为 `structured_rebuilt`。KV 与 R2 双模式均已通过正式迁移重放及六类失败场景，真实旧系统数据副本和 Cloudflare Free 远程资源仍需演练。
- 第二十六批用户注销与期满清理纵向切片已经完成本地闭环，正式迁移为 `0020-用户生命周期与期满清理.sql`；唯一管理员可以原子转让身份，用户申请注销后立即撤销普通会话，并通过独立 30 分钟恢复会话在七天内取消注销。
- 用户与组织期满清理复用删除操作、七步记录、父子操作关联、后台任务和租约账本；组织删除可以在七天内恢复。Cron 会为升级前已经处于待删除状态但缺少正式账本的组织保留原截止时间并补建任务，避免旧数据永久停留。
- 第二十七批收信控制与全域认领纵向切片已经完成本地闭环，正式迁移为 `0021-收信控制与全域认领.sql`；管理员可以暂停域名、地址或用户收信，管理四类基础拒收规则，并按域名切换拒收未知地址或保存为未分配来信。
- 未分配来信只向当前获授权用户开放独立列表、详情和附件；认领在单个 D1 原子批次内建立个人别名、关闭当前地址时期、迁入历史邮箱条目、计入逻辑用量并登记搜索、会话和审计。当前本地验证保护线为单次 30 封，真实 D1 验证前不能写成最终产品限制。
- 内容关键词规则需要在持久化前受限解析，同时 Queue 仍执行正式完整性解析，因此同一邮件可能解析两次。真实 Workers Free Email Routing 与 Queue consumer 的接近 20 MB CPU、内存、并发与重试成本仍是上线门槛。
- 第二十八批网页新邮件自动更新纵向切片已经完成本地代码、自动化和质量门槛；本批没有新增 D1 迁移、Cloudflare 组件或部署方式。
- 页面可见且在线时每 60 秒静默刷新当前普通邮箱或未分配邮箱视图；登录、切回页面和网络恢复后立即检查，后台或离线时暂停，连续失败按 2 分钟、5 分钟和 10 分钟上限退避，成功后恢复 60 秒。
- 自动刷新不清空当前勾选、提示或正在阅读的邮件，手动刷新始终可用。可见页面两次真实本地邮件投递分别在 40 秒和 30 秒后自动更新，正在阅读的邮件保持打开。当前实现重载当前完整列表；原生后台、离线、登录与登出生命周期及真实 Workers Free 请求和 D1 读取成本仍需验证。
- 第二十九批管理员运行健康状态纵向切片已经完成本地实现，正式迁移为 `0022-运行健康与定时维护心跳.sql`；唯一系统管理员可以只读查看收信、发信、存储和定时维护四类 D1 账本摘要，普通用户访问返回 `403`。
- 健康状态使用“正常、需检查、尚无记录、未配置”，不会读取邮件正文、附件、地址明文或密钥，也不会因查看页面主动探测外部服务。每次 Cron 保存开始、成功或失败心跳，失败只记录受限步骤和安全错误代码。
- 2026-08-14 已补齐健康页桌面及 `390 × 844` 手机刷新交互、布局和控制台复核；手机页面及四块状态卡没有横向溢出，桌面与手机控制台均无警告或错误。真实 Cron 和远程资源交叉验证仍是上线门槛。
- 备份创建时读取 `d1_migrations` 的实际最新版本并冻结到备份运行，最终清单沿用该版本；新增迁移不再需要修改备份版本常量。
- 第三十批建立升级、数据检查与回退演练工具；当前 `tools/本地升级回退自检.mjs` 从 `0023` 依次升级到 `0025`，验证 25 个正式迁移、115 张既有表行数、代表性数据摘要、外键、逻辑配额与内部投递结构及独立回退目标。
- 正式迁移只向前追加。代码回退只用于版本说明明确兼容且数据检查通过的情况；数据或结构不兼容时，必须把升级前完整备份恢复到迁移版本一致的全新空白 D1 与 KV/R2，六项检查通过后再切换 Worker 绑定，不在生产库执行通用逆向 SQL。
- 当前升级手册位于 `文档/运维/升级与回退手册.md`，`0022` 到 `0023` 的具体行为、配置、数据、检查和回退说明位于 `文档/发布/0023升级说明.md`。真实 Cloudflare Free 远程升级与绑定切换仍待演练。

- 需求定义阶段已经完成，十二个需求模块和八项跨模块收口内容均已确认，最终一致性检查已经通过。
- `需求基线-0001` 已由用户最终接受并成为当前需求基线，首发验收清单已经生效。
- 需求变更 `0001` 至 `0012` 均已接受并生效；当前共 161 项有效需求。`2.09` 与 `2.10` 已明确 D1、KV、R2 免费额度是产品资源上限，管理员查看已用免费额度、总上限和剩余额度；`7.16` 要求管理员查看并设置域名月度发件配额；`10.06` 已明确首发域外发信只支持 Resend 和 SMTP2GO；需求变更 `0005` 与 ADR `0034` 明确 Simlettra 不重复验证邮件域名所有权；需求变更 `0006` 与 ADR `0035` 明确首发邮箱地址字符和规范化规则；需求变更 `0007` 与 ADR `0036` 明确新用户个人别名默认额度为 20；需求变更 `0008` 与 ADR `0038` 明确新用户组织创建默认额度为 5；需求变更 `0010` 与 ADR `0046` 明确管理员生成可反复展示、可撤销、无有效期的一次性账号邀请码并绑定注册域名；需求变更 `0011` 与 ADR `0049` 明确可信管理员密钥边界和 Cloudflare 连接 GitHub 默认部署方式；需求变更 `0012` 与 ADR `0052` 明确 D1、KV/R2 和 Queue 按 Worker 项目名称自动创建或复用。两类额度分别使用独立用户策略管理。
- 架构约束、旧系统架构观察、Cloudflare 平台核验和第一批目标架构已经建立。
- 第一批整体架构已由用户接受，架构决策记录 `0001` 至 `0006` 均为“已接受”。
- 已接受的核心技术路线为：单 Worker 模块化单体、D1 权威状态、KV/R2 对象存储、Cloudflare Queue 异步任务、R2 默认存储，以及 TypeScript、Vue 3、Vite、Cloudflare 官方 Vite 插件和仅用于 HTTP 层的 Hono。
- 中文搜索与 100,000 封邮件规模的本地验证性原型已经完成，结果记录于 `文档/验证/中文搜索与D1规模验证结果.md`。
- 第二批搜索架构已由用户接受：采用中文双字词元、首发正文搜索至少输入两个汉字、使用邮箱范围词元缩小候选集合，并使用内容空 FTS5 索引；ADR `0007` 状态为“已接受”。
- 接近 20 MB 邮件解析的本地验证已经有限通过，结果记录于 `文档/验证/接近20MB邮件解析验证结果.md`；真实 Workers Free Queue consumer 的内存、CPU、并发和重试仍需远程复核。
- 第三批 MIME 解析架构的通用结论继续有效：20 MB 按 20,000,000 个原始邮件字节计算，通过适配器使用 `postal-mime`，以 `ArrayBuffer` 解析并限制恶意结构，解析失败保留原始邮件。ADR `0030` 已取代 ADR `0008` 中“完整 20 MB 以 Workers Paid 为生产基线”的结论，当前以真实 Workers Free 验证作为上线门槛。
- D1 与 KV/R2 故障恢复的本地验证已经通过，结果记录于 `文档/验证/KV与R2故障恢复验证结果.md`；真实 Cloudflare 的 KV 多地区一致性与对象对账成本仍需复核。
- 第四批对象故障恢复架构已由用户接受：对象不跨邮件共享，D1 保存对象登记与哈希，全部对象校验后原子提交可见，KV 增加一致性等待，衍生对象换新键修复，永久删除先撤销访问再后台清理，孤立对象经过保护期和重复对账后删除；ADR `0009` 状态为“已接受”。
- 历史三种域外发信服务网关契约原型已经通过九组本地场景；其通用状态机、MIME 字节边界、重试、防重和乱序证据仍可参考，但三家 Provider 范围已经由 ADR `0028` 取代。
- 当前域外发信架构只支持 Resend 和 SMTP2GO：最终邮件按 Simlettra、供应商和收件条件的较小上限执行；每个站外收件人保存独立状态；Resend 使用 24 小时幂等键；SMTP2GO 结果未知时不自动重发；两家事件分别使用 Svix 和 HTTPS Basic Auth 边界。ADR `0010` 的通用结论继续有效，Provider 范围以 ADR `0028` 为准。
- 密码与会话的本地验证已经通过，九组场景覆盖 PBKDF2 与 Argon2id 候选、密码记录、虚拟哈希、会话摘要、撤销、Cookie、CSRF、过期和登录限速；结果记录于 `文档/验证/密码与会话验证结果.md`。
- 第六批密码与会话架构已由用户接受：使用平台原生 PBKDF2-HMAC-SHA-256 总计 900,000 次并分为每轮不超过 100,000 次的链式派生、版本化密码记录、可撤销不透明会话、7 天空闲与 30 天绝对有效期、受保护 Cookie、CSRF 令牌和双维度登录限速；真实 Workers 已确认单次超过 100,000 次会失败，ADR `0011` 已由 ADR `0053` 取代。
- D1 数据库工具与迁移的本地验证已经通过，11 组场景覆盖 Drizzle 与原生查询一致性、权限范围、键集分页、FTS5、批处理、参数分块、Prisma 候选、失败迁移和逻辑恢复；结果记录于 `文档/验证/D1数据库工具与迁移验证结果.md`。
- 第七批数据库工具、迁移与备份架构已由用户接受：采用 Drizzle 普通查询层、Wrangler 唯一迁移账本、原生 D1 特殊能力、键集分页、90 参数分块，以及排除 FTS5 后可重建索引的权威数据逻辑备份；ADR `0012`、`0013` 状态均为“已接受”。
- 项目级 Skill 位于 `.agents/skills/simlettra/`，名称为 `$simlettra`，已经通过结构校验。
- 正式数据模型六批设计已经完成，设计总纲位于 `文档/数据模型/数据模型设计总纲.md`。
- 第一批数据模型已由用户接受：账号、地址身份和当前地址占用相互分离，系统管理员与组织权限从显式主体关系推导；ADR `0014`、`0015` 状态均为“已接受”。需求变更 `0005` 与 ADR `0034` 已移除域名“等待验证”状态和验证时间字段，管理员添加的合法域名直接启用。
- 第一批具体设计和迁移草案已经建立。完整草案包含 13 张表，本地 SQLite 的 17 个约束场景全部通过且外键检查为零；初始化切片所需的 10 张基础表已经审查进入正式迁移 `0001-系统身份与地址基础.sql`，其余结构随对应纵向切片继续迁入。
- 第二批数据模型已由用户接受：物理邮件、实际投递、个人与组织邮箱条目、成员个人状态相互分离；会话按权威关系在邮箱范围内重建；未分配来信按地址时期隔离认领历史。ADR `0016` 至 `0018` 状态均为“已接受”。
- 第二批具体设计和迁移草案已经建立。草案新增 13 张表，与第一批顺序执行后共 26 张表；本地 SQLite 的 48 个约束、权限、认领、跨表匹配和删除场景全部通过且外键检查为零。草案尚未进入正式 `migrations/`，真实 D1、Drizzle 对齐、应用服务授权和目标规模性能仍需验证。
- 第三批数据模型已由用户接受：草稿属于实际编辑者并以修订号和冲突副本防止覆盖；一次混合发送只建立一个发送操作、一封物理邮件和一个已发送条目；每名收件人、供应商尝试和不可变事件独立维护状态、幂等与重试历史。ADR `0019` 至 `0021` 状态均为“已接受”。
- 第三批具体设计和迁移草案已经建立。草案新增 12 张表，与前两批顺序执行后共 38 张表；本地 SQLite 的 71 项结构、约束、跨表匹配、生命周期、状态历史、幂等和删除验证断言全部通过且外键检查为零。草案尚未进入正式 `migrations/`，真实 D1、Drizzle 对齐、Queue 状态机、应用服务事务和目标规模性能仍需验证。
- 第四批数据模型已由用户接受：KV/R2 使用统一逐对象登记和对象代数；邮件完整性是邮箱可见的独立门槛；收信使用来源事件或有界指纹防重并冻结接受时路由；D1 后台任务使用租约、递增领取令牌、任务尝试和分批对象对账。ADR `0022` 至 `0024` 状态均为“已接受”。
- 第四批具体设计、迁移草案和可重复验证原型已经建立。草案新增 8 张表，与前三批顺序执行后共 46 张表；本地 SQLite 的 63 项对象完整性、收信、最终 MIME、任务租约、对账和失败事务断言全部通过且外键检查为零。草案尚未进入正式 `migrations/`，真实 D1、KV/R2、Queue 并发、Drizzle 对齐、20 MB 对象处理和目标规模性能仍需验证。
- 第五批数据模型已由用户接受：Provider 与通知凭据由独立于 `init_key` 的配置加密主密钥保护；每个域名使用默认和备用 Provider 路线；每名站外收件人独立选择兼容服务；明确未接受时可切换，结果未知时停止自动动作；通知、外部邮箱验证和个人来信转发使用独立防重操作。ADR `0025` 至 `0028` 状态均为“已接受”，Provider 类型只允许 Resend 和 SMTP2GO。
- 第五批具体设计、迁移草案和可重复验证原型已经建立。草案新建 20 张模型表并重建 4 张发送核心表，移除被取代的 7 张临时表后，五批顺序执行共 63 张表；本地 SQLite 的 49 项路线、切换、凭据形状、通知、验证、转发、防重和环路断言全部通过且外键检查为零。草案尚未进入正式 `migrations/`，真实 D1、Web Crypto、Queue 并发、Drizzle 对齐、Provider 和通知通道仍需验证。
- 第一批正式工程骨架已经完成：使用一个根级 pnpm 包，建立 `src/worker`、`src/web`、`src/modules`、`src/shared`、`migrations`、`tests` 和 `tools` 边界；同一 Worker 导出 `fetch`、`email`、`queue` 和 `scheduled` 入口，Vue 网页通过 Hono HTTP 边界读取系统状态。
- 第二批初始化纵向切片已经完成：正式迁移 `0001` 建立十张基础业务表；服务端使用 `init_key` 限速鉴权、PBKDF2-HMAC-SHA-256 和 D1 原子批次建立唯一管理员、首个域名与主邮箱；Vue 提供三步初始化向导；安全构建隔离并扫描本地 `.dev.vars`。
- 第三批密码登录与会话纵向切片已经完成：正式迁移 `0002` 建立 `sessions` 和 `login_rate_limits`；服务端提供主邮箱密码登录、当前会话、会话列表、退出和指定会话撤销接口；会话令牌与 CSRF 令牌只以摘要进入 D1，使用受保护 Cookie、同源与 CSRF 校验、七天空闲与三十天绝对有效期，并按账号和来源双维度限制登录失败。
- Vue 网页能够在初始化完成后进入管理员登录，刷新恢复有效会话，显示账号和会话列表，并退出当前或其他会话。桌面与 `390 × 844` 手机浏览器流程已经通过，旧会话撤销后不能恢复，控制台没有警告或错误。
- 第四批密码管理与恢复纵向切片已经完成：正式迁移 `0003` 建立只追加的 `audit_events`；普通用户可以验证当前密码后改密并选择是否退出其他设备，管理员可以为普通用户生成 24 小时有效且只展示一次的临时密码，临时登录形成受限会话并强制设置正式密码，管理员也可以通过部署配置中的 `init_key` 恢复密码且不换取登录会话。
- 密码写入、会话撤销和成功审计通过一个 D1 原子批次提交；临时密码过期、受限会话边界、CSRF、越权、审计失败回滚和并发改密竞争已有自动化覆盖。桌面和 `390 × 844` 手机页面无横向溢出，浏览器控制台无警告或错误。
- 第五批用户创建与状态管理纵向切片已经完成：管理员可以查看当前用户和已启用域名，原子创建账号、24 小时临时密码、主地址、地址占用与默认地址偏好，并禁用或重新启用普通用户；禁用同时撤销全部目标会话，唯一管理员受服务端保护。
- 用户创建的地址冲突与审计失败回滚、权限边界、临时密码登录、禁用会话失效和重新启用已有自动化覆盖；桌面与 `390 × 844` 手机浏览器完成真实创建和状态切换验收，测试数据已经清理且外键检查为零。
- 第六批邮件域名管理纵向切片已经完成：管理员可以查看、直接添加、暂停、恢复和删除邮件域名；域名采用统一 IDNA/Punycode 规范化且不重复验证所有权，暂停域名停止新地址分配，永久删除要求二次确认并在服务端拒绝仍有关联地址的域名。
- 域名管理的权限、CSRF、规范化、重复冲突、状态切换、分配限制、删除阻止和审计失败回滚已有自动化覆盖；桌面浏览器完成真实流程验收，域名区域已经在后续 `390 × 844` 整页复核中确认无横向溢出。
- 第七批个人地址与别名管理纵向切片已经完成：正式迁移 `0004` 建立 `user_alias_policies` 和数据库额度准入触发器，为已有用户回填 20 个默认额度；初始化与创建用户原子建立策略，用户可以自助创建、整理、设置默认发件和删除个人别名，管理员可以设置策略并为用户分配或删除别名。
- 个人地址管理的默认策略回填、数据库额度保护、地址冲突、暂停域名、策略开关、额度调低、管理员分配、偏好、排序、默认发件、删除释放、权限、CSRF 和审计失败回滚已有自动化覆盖；桌面与 `390 × 844` 手机浏览器完成真实流程验收，测试数据已经清理且外键检查为零。
- 第八批地址策略与别名保留期纵向切片已经完成：正式迁移 `0005` 建立全局地址规则、删除操作和带租约的 D1 权威任务；管理员可以设置邮箱前缀最短长度、禁止文字、保留名称和 0 至 30 天别名保留期，策略只约束新地址。
- 个人别名删除会立即结束归属，并按删除时冻结的策略立即释放或保留占用；Queue consumer 处理到期释放，每小时 Cron 补投遗漏任务，重复消息没有重复副作用。桌面与 `390 × 844` 手机浏览器完成真实流程验收，本地测试数据已经清理且外键检查为零。
- 第九批组织身份与成员协作纵向切片已经完成：正式迁移 `0006` 建立独立用户组织策略、成员关系和系统内邀请；初始化管理员、已有用户与新建普通用户默认最多创建 5 个组织，管理员可以逐用户调整额度。
- 用户可以在额度内原子创建组织、唯一共享地址和创建者成员关系，管理三类邀请偏好、邀请现有用户、接受或拒绝邀请、查看成员、设置普通成员发信权限，并按规则退出、继承、删除和恢复组织。桌面与 `390 × 844` 手机浏览器完成真实流程验收，本地测试数据已经清理且外键检查为零。
- 第十批邮件接收与完整性提交纵向切片已经完成：正式迁移 `0007` 建立物理邮件、实际投递、邮箱条目、对象登记、完整性状态、收信操作、冻结路由和防重关系；Worker `email` 入口先保存原始 MIME，再由 D1 权威任务和 Queue 使用 `postal-mime 2.7.6` 解析及校验对象。
- R2 与 KV 两种存储契约、个人与组织路由、一小时有界防重、20,000,000 字节应用上限、最终 D1 原子可见提交和故障重试已有高保真自动化覆盖；真实 Workers Free 的接近 20 MB 解析和 KV 多地区一致性仍是上线门槛。
- 第十一批收件箱与安全读信纵向切片已经完成：正式迁移 `0008` 建立每名用户的稀疏邮箱状态和完整地址级可信发件人；统一收件箱按个人归属与当前组织成员关系授权，提供稳定分页、邮件详情、独立已读、附件下载与常见图片预览。
- HTML 邮件使用锁定版本 `DOMPurify 3.4.13` 净化并放入没有脚本、表单和同源权限的隔离 `iframe`；远程图片默认移除地址，只能由用户对单封邮件或完整发件地址放行。桌面与 `390 × 844` 手机浏览器完成真实邮件验收，无横向溢出或控制台错误。
- 第十二批邮箱整理与垃圾箱纵向切片已经完成：复用 `mailbox_user_states` 提供收件箱、已发送、星标、归档、垃圾邮件、垃圾箱和全部邮件视图，以及单封和最多 50 封批量整理；组织成员状态彼此独立。
- 移入垃圾箱会冻结原位置和 30 天到期时间；到期后查询立即拒绝访问。网页使用锁定版本 `@lucide/vue 1.31.0` 的按需图标，桌面与 `390 × 844` 手机浏览器均已验收。
- 第十三批邮件永久删除纵向切片已经实现：用户可以二次确认永久删除自己的个人邮箱副本；普通组织成员不能永久删除组织原始邮件，只有组织创建者可以让所有成员立即失去访问。30 天到期后个人邮件自动进入同一删除流程，组织成员状态只对该成员永久隐藏。
- 永久删除复用 `deletion_operations`、分步记录和统一后台任务；物理邮件仍有其他邮箱引用时保留共享正文与附件，最后一份引用消失后逐个删除 KV/R2 对象并清理 D1 关系。对象删除未生效或暂时失败时保留账本并安全重试，审计不保存邮件内容或对象键。
- 第十三批网页已经完成二次确认、个人与组织影响说明和结果反馈；桌面端已验证影响说明、确认门槛与取消操作，`390 × 844` 手机端已完成永久删除及共享引用保留提示，页面无横向溢出，控制台无警告或错误。
- 第十四批邮箱组合搜索纵向切片已经实现：正式迁移 `0009` 建立邮件搜索状态、正文分块与内容空 FTS5 索引；用户可以组合搜索正文、主题、发件人、收件人、实际投递地址、日期、附件和个人状态，并按时间、未读、星标或附件优先排序。
- 正文索引使用中文相邻双字词元和哈希邮箱范围词元，索引任务可以补建、重建和安全重试；实际结果仍重新检查个人归属与当前组织成员资格。永久删除已同步清理索引数据，恢复流程以后可以调用全量重建。
- 第十四批网页已经完成主搜索框、组合条件、排序、清除和索引状态提示；2026-08-14 已补齐桌面与 `390 × 844` 手机展开、主题筛选、清除、布局和控制台复核，页面没有横向溢出或控制台错误。真实 Cloudflare D1 搜索规模与跨浏览器恶意样本仍待验收。
- 第十五批邮件会话归并纵向切片已经实现：正式迁移 `0010` 建立权威邮件关系、个人或组织邮箱范围会话和会话成员；MIME 解析标准 `Message-ID`、`In-Reply-To` 与有序 `References`，收信可见事务同时保存关系事实和 D1 权威重建任务。
- 会话后台任务支持晚到父邮件和派生表全量重建；仅主题相同不会合并，重复外部标识不会解析或共享会话根。列表按当前视图或搜索结果折叠会话并显示封数，详情按时间列出当前有权邮件，每封邮件的状态和操作仍保持独立。
- 第十五批高保真自动化已覆盖关系解析边界、任务重试、同主题分离、晚到父邮件、重复标识隔离、个人与组织范围授权、成员退出和列表及详情契约。桌面与 `390 × 844` 手机会话补验已通过；自发自收的收件箱和已发送条目在详情中按物理邮件去重。
- 第十六批草稿与写信编辑纵向切片已经完成：正式迁移 `0011` 建立个人草稿、当前内容、收件人、附件和幂等变更，并扩展统一对象登记支持草稿正文与附件；用户可选择当前有权地址，以富文本或纯文本跨设备编辑。
- 自动保存使用修订号、变更防重键和输入摘要；过期编辑形成可见冲突副本。附件通过大小与 SHA-256 回读校验后才进入当前修订。迁移已在全新及带历史收信数据的 D1 上验证，桌面和 `390 × 844` 手机浏览器流程无溢出或控制台错误。
- 第十七批正式发信纵向切片已经实现：正式迁移 `0012` 建立 Provider 配置与版本、域名默认和备用路线、发送操作、逐收件人状态、Provider 尝试与事件、每日及月度发件额度和预留记账；一次发送只建立一封物理邮件和一个已发送条目。
- 正式发送会重新验证草稿精确修订、发件权限、最终 MIME、20,000,000 字节上限和配额；系统内地址原子直投，站外地址通过 Resend 或 SMTP2GO 异步投递。只有 Provider 明确拒绝接受时才切换备用服务，结果未知不会自动重发。
- 唯一系统管理员可以查看和管理加密保存的 Provider 明文凭据、域名路线与额度；Resend Svix 和 SMTP2GO Basic Auth 回调边界已经实现。桌面及 `390 × 844` 手机浏览器真实内部发送已通过，真实 Provider 账号和 Workers Free 远程能力仍是上线门槛。
- 第十八批回复与转发纵向切片已经完成：用户从有权邮箱条目建立回复、回复全部或转发草稿；服务端重新检查来源访问权，优先使用 `Reply-To`，回复全部去除用户个人地址并按组织发信权限保留共享地址。
- 回复与回复全部生成 `In-Reply-To`、完整 `References` 和权威邮件关系；转发复制普通附件但不加入原会话。不可信 HTML 只提取可见文字并转义为安全引用。桌面及 `390 × 844` 手机站内回复流程已通过。
- 第十九批外部通知网关纵向切片已经完成：正式迁移 `0013` 建立用户通知订阅、范围、加密凭据、权威通知操作和尝试历史；用户可以为个人邮箱和当前有权组织邮箱建立 ntfy、Gotify、WxPusher、Telegram 或 Bark 订阅，并暂停、恢复或删除自己的订阅。
- 外部来信和系统内直投都在邮件完整可见后建立防重通知任务；任务重新检查当前权限、正文对象和冻结摘要，只推送发件人、收件人、抄送、实际投递地址、主题和完整安全正文，不发送附件。正文超限、权限失效、失败或结果未知均不会影响原邮件。桌面与 `390 × 844` 手机浏览器设置流程已通过，真实通知服务账号仍待远程联调。
- 第二十批外部邮箱验证与自动转发纵向切片已经完成：正式迁移 `0014` 建立外部邮箱目标、一次性验证、验证邮件尝试、规则版本与范围、独立转发操作和 Provider 尝试；用户可以验证外部邮箱，并转发全部个人来信或指定个人地址来信。
- 验证明文只短暂存在于当前 Worker 和邮件中；D1 只保存带随机盐的摘要。转发在原邮件完整可见事务中冻结规则、投递、原始大小、内容摘要和路线，Queue 执行前重新检查权限，使用独立防重与环路保护；失败或结果未知不影响原邮件，只有 Provider 明确未接受时才切换备用服务。桌面与 `390 × 844` 手机浏览器设置流程已通过，真实 Provider 账号和 Cloudflare Queue 并发仍待远程联调。
- 第二十一批管理员免费资源用量与保守停止纵向切片已经完成：正式迁移 `0015` 建立 Cloudflare 只读配置、资源阈值版本、不可改写用量快照和增长前容量预留；管理员可以查看 D1 与当前 KV/R2 的账号总量和当前资源双口径，并设置预警与停止比例。
- D1 同时受 Free 账号 5 GB 与当前单数据库 500 MB 限制，KV 同时按账号和当前命名空间 1 GB，R2 按产品约定的账号和当前存储桶 10 GB 免费容量口径；只使用本地估算时有效停止比例最高为 `80%`。收信、草稿附件和已发送副本必须同时通过账号及当前资源容量预留，达到停止线后阅读、导出、备份和永久删除仍可继续。
- 第二十一批高保真测试覆盖迁移、管理员权限、加密凭据、官方与本地快照、阈值、容量预留及三个增长入口；桌面和 `390 × 844` 手机浏览器已确认双口径说明、长输入、阈值操作和无横向溢出，控制台没有警告或错误。真实 Cloudflare Free 账号接口、权限组合和指标延迟仍是上线门槛。
- 当前自动化基线为 56 个测试文件、234 项测试；第三十八批邀请码注册、第三十九批数据库兼容与会话去重、第四十六批多地址收信及备份迁移版本回归均已通过。本地 KV/R2 完整恢复、本地 KV/R2 旧系统迁移、两种早期逻辑配额形状和 `0023` 到 `0025` 升级回退自检已通过。格式、代码规则、Cloudflare 类型生成、网页/Worker/配置/测试类型检查、R2/KV 生产安全构建和 Wrangler 部署预检均已通过。
- 第三十一批首发本地验收证据收口已经完成，第三十二批组织发信与回复可见性对照回归已经完成；第四十批后当前 28 项首发验收为 8 项“本地通过”、1 项“本地自动化通过”、18 项“部分通过”和 1 项“未执行”。
- 正式 HTTP 回归确认组织创建者始终可以使用共享地址发信，普通成员在组织开关关闭时被拒绝、开启后可以实际发送；个人地址回复组织邮件时，移除组织地址只产生个人邮箱条目，保留组织地址会产生当前成员可读的组织邮箱条目。
- 第三十三批建立的本地安全验收现由 `pnpm test:security` 聚合 8 个文件、52 项安全回归和生产安全构建；登录响应、真实邀请码失败路径、恶意附件名、重复投递、发送、多地址归并与 Queue 失败重投均有专门回归。完整发布安全检查仍需跨浏览器扩展样本、真实日志总审计、外部故障注入和人工签署。
- 第三十四批建立的首发性能验收工具已使用全部 25 个正式迁移重新验证：50 名用户、100,000 封邮件和 10 个读取线程下，两次运行较保守的列表第 95 百分位为 258.41 ms、详情为 3.22 ms。读取线程设置 5 秒 SQLite 忙等待以吸收并发启动瞬时锁，不改变查询阈值。该结果只代表本地 D1 数据层，验收-26仍为“部分通过”。
- 第三十五批真实远程验收准备已经完成：新增 `wrangler.kv.jsonc`、R2/KV 双模式构建部署命令、远程运行手册和证据模板；两种最终产物均通过 Wrangler `deploy --dry-run`。真实资源编号仍是占位值，没有执行远程创建、迁移或部署。
- 第三十六批非程序员部署与初始化文档已经完成：新增 R2/KV 首次部署、双密钥、三步网页初始化、Email Routing 和首次自检指南，以及按现象组织的基本排错、停止条件与脱敏记录模板；命令已经与 Wrangler `4.120.1` 和当前实现核对，尚未由真实非程序员在全新环境中执行。
- 第三十七批首发发布资料与剩余门槛收口已经完成：`文档/发布/0.1.0开发版说明.md` 记录整版能力、配置、数据影响和已知限制，`文档/发布/首发发布门槛.md` 当前维护 23 项仍开放的真实环境、外部服务、运维、浏览器、安全和真人验收阻断项；当前发布判断明确为“不发布”。
- 第三十八批账号邀请码注册已经完成本地闭环，正式迁移为 `0023-账号邀请码注册.sql`。管理员为指定域名生成、反复查看、复制和撤销无有效期的一次性邀请码；受邀人填写可用邮箱前缀、显示名称、密码和时区注册，成功后原子建立正式账号并立即消耗邀请码。
- 邀请码摘要和 AES-GCM 加密副本进入 D1，管理员页面可以反复查看完整邀请码；来源限速、域名状态、地址冲突、并发使用、失败回滚和审计边界均由服务端及数据库保护。桌面与 `390 × 844` 手机浏览器已经完成生成、注册、使用状态和撤销验收。
- 第三十九批早期数据库兼容与会话去重已经完成本地闭环，正式迁移为 `0024-修复早期逻辑配额表兼容性.sql` 和 `0025-补齐早期内部投递容量拒绝事实.sql`。两种早期表形状和当前形状均可安全升级，策略、预留、用量、约束及触发器得到保留或恢复。
- 自发自收仍保留收件箱和已发送两个邮箱条目，会话详情按 `message_id` 只展示一封物理邮件并优先保留当前打开条目。手机端草稿保存、站内发送、回复和两封物理邮件会话已经通过 `390 × 844` 浏览器补验。
- 第四十批本地浏览器视觉补验已经完成永久删除、全域认领、个人与组织导出，以及可见页面自动更新和保持阅读状态的真实本地流程。验收-09 已更新为“本地通过”；自动更新的原生后台与离线生命周期继续保留为发布阻断项。
- 第四十一批账号邀请码日志脱敏复核已经完成：使用真实邀请码触发注册审计失败，确认完整值及无连字符值都不会进入失败响应、审计字段或控制台输出；该回归继续包含在当前 8 个文件、52 项测试的发布前安全聚合中。真实 Cloudflare 与第三方平台日志总审计仍是发布阻断项。
- 第四十二批网页控件可访问性自检已经完成本地结构闭环：Vue AST 共检查 1,723 个模板元素、338 个原生交互控件，当前收件箱运行时 39 个可见控件均有名称；没有非原生点击、正数 `tabindex` 或缺少 `type` 的按钮。真实键盘操作及 Chrome、Edge、Firefox、Safari 浏览器矩阵仍是发布阻断项。
- 第四十三批发布前安全人工检查准备已经完成：新增可按 R2/KV 模式分别填写和签署的检查表，覆盖权限、不可信内容、凭据与日志、重复重试、外部边界、部署配置和依赖；远程手册、证据模板、首次部署和升级回退口径已统一到当前 `0025`。检查表尚未在真实环境执行，发布-21和发布-22继续保持开放。
- 第四十四批生产依赖安全审查已经完成：2026-08-14 通过 npm 官方公告只读审计 38 个生产及可选依赖，所有严重级别均为零；355 个完整依赖中只有 `drizzle-kit -> @esbuild-kit -> esbuild 0.18.20` 的一项中等开发服务器风险。该链不进入正式开发服务或生产产物，当前禁止运行或对外开放 Drizzle Studio，不使用不受上游支持的强制覆盖；正式发布前必须重新审计。
- 第四十五批部署配置静态安全审查已经完成：R2 与 KV 模板产物均通过绑定、存储模式、Queue、Cron、网页回退和 Wrangler `deploy --dry-run` 检查；生产构建会清除生成配置中的本机绝对路径，严格部署会在 Wrangler 前拒绝全零 D1 或 KV 编号。真实 Cloudflare 资源、Free 套餐、远程权限和密钥仍待人工验收。
- 第四十六批多地址收信归并与补交付已经完成：同一窗口、信封发件人和原始 MIME 的多地址事件共享一封物理邮件；同一用户多个地址只建立一个邮箱条目，不同用户分别可见。可见后迟到路由通过 `receive_route_commit` D1 任务和 Queue 补交付，两类 Queue 发送失败重投已有回归；真实 Cloudflare Free 多地址 Email Routing 仍待验收。ADR `0047` 已局部取代 ADR `0023` 的旧指纹组成。
- 第四十七批 Cloudflare 多地址收信事件语义核验已经完成：官方只确认 `ForwardableEmailMessage.to` 是单个信封 `RCPT TO`，未承诺一个多收件人 SMTP 事务的 Worker 调用数量、顺序、重试身份或各次原始 MIME 字节一致性。远程运行手册和证据模板已加入五类多地址矩阵与停止条件，真实 Free 验证继续阻断验收-06。
- 第四十七批最终收口时，184 份 Markdown 的 525 个内部链接已检查，失效链接和 UTF-8 BOM 均为零。
- 第四十八批可校验源码快照已经完成：`pnpm package:source` 使用显式根目录白名单、递归隐藏目录排除、本地密钥值扫描、固定 ZIP 时间、稳定路径排序、两次生成一致性和解压逐项哈希校验，当前已经生成 `0.1.0-dev.0` 开发版源码 ZIP、外部清单与 SHA-256。
- 第四十八批最终收口时，185 份 Markdown 的 529 个内部链接已检查，失效链接和 UTF-8 BOM 均为零。
- 第四十九批已经在新项目根目录初始化空白 `main` Git 仓库，并在仓库级设置 `core.quotepath=false` 以直接显示简体中文路径；`.gitignore` 已排除本地密钥、依赖、构建产物、日志、临时数据、开发快照和原型生成结果。当前没有提交、标签或远程推送，发布-17继续保持开放。
- 第四十九批最终收口时，Git 可纳管文件与开发源码快照均为 532 个，双向路径差异和敏感或生成路径误纳入均为零；186 份 Markdown 的 533 个内部链接已检查，失效链接和 UTF-8 BOM 均为零。
- 开发快照不能替代正式不可变版本标识和可取得的上一正式版本；未经用户明确要求，不建立首次提交、标签或发布。
- 第五十批首次真实 Cloudflare R2 部署已于 2026-08-13 完成到隔离资源：新建独立 D1、R2 和 Queue，生产 D1 已按顺序应用 `0001` 至 `0025`，外键检查为零；单 Worker 已绑定 D1、R2、Queue、每小时 Cron 和网页资源。当日使用 Secret 和本机 DPAPI 的密钥做法已经被需求变更 `0011` 与 ADR `0049` 取代，只作为历史部署事实保留。
- Wrangler `4.120.1` 的远程 `d1 migrations apply` 命令字符串通道在正式触发器迁移处返回 `incomplete input`，但同一未修改 SQL 通过 D1 文件导入成功。ADR `0048` 已接受，`tools/远程正式迁移.mjs` 使用文件导入并沿用唯一 `d1_migrations` 账本；空白远程 D1 的 25 个迁移、重复运行、零外键和故意失败整体回滚均已验证。
- 当前线上 R2 实例仍处于 `not_initialized`：网页初始化向导在桌面与 `390 × 844` 手机视口正常、无横向溢出或控制台错误，但尚未建立管理员、首个域名和主邮箱，也未配置 Email Routing。不得把“Worker 已部署”表述为“邮箱已经可收发”或“首发门槛已全部通过”。
- 需求变更 `0011`、`0012` 与 ADR `0049`、`0051`、`0052` 已接受：配置主密钥绑定统一改名为 `CONFIG_KEY`；唯一系统管理员可在 Cloudflare 控制台查看和管理 `INIT_KEY` 与 `CONFIG_KEY`，D1 动态凭据加密不用于防范管理员；正式发布默认通过 Cloudflare 创建 Worker 页面连接 GitHub，保持 `pnpm run build` 与 `npx wrangler deploy` 默认命令，构建变量只选择存储模式。构建入口按实际 Worker 项目名称自动创建或复用 `simlettra-<Worker项目名称>-meta` D1、`simlettra-<Worker项目名称>-raw` KV/R2 和 `simlettra-<Worker项目名称>-tasks` Queue，再生成真实配置并保护 D1 迁移边界；已有系统迁移不一致时自动停止。
- 公开部署仓库 `wonly211/Simlettra` 的当前部署提交为 `ded2f43dddfbaee522edf8b232435482a381f541`。首次 Cloudflare Git 构建曾因旧入口未读取构建变量而使用全零 D1 编号并返回 `10181`；第二次构建已按规则成功创建 `simlettra-simlettra-meta` D1、`simlettra-simlettra-raw` R2 和 `simlettra-simlettra-tasks` Queue，但新 D1 自带的 Cloudflare 保留表 `_cf_KV` 被误判为业务数据。当前修复只忽略精确保留表 `_cf_KV`，未知 `_cf_*` 与真实业务表仍触发安全停止；最新版本还会以不包含请求正文、附件和密钥的摘要记录未预料 HTTP 错误，便于排查初始化失败；冻结锁文件、R2/KV 完整 `pnpm check`、命名创建与复用模拟、D1 保留表边界、缺少存储模式提前停止和公开内容扫描均已通过，修复后的真实 Cloudflare 构建仍待确认。
- 第五十一批可信管理员密钥与 GitHub 部署调整已经完成：运行代码、测试、类型和模板只使用 `CONFIG_KEY`，三份正式 Wrangler 配置均启用 `keep_vars`；本机 DPAPI 文件与复制脚本已经删除；R2/KV GitHub 部署配置自检、56 个测试文件的 234 项测试及全部独立质量门槛均已通过。2026-08-14 已把隔离线上 Worker 更新到版本 7，`INIT_KEY` 与 `CONFIG_KEY` 均为管理员可查看的 `plain_text`，Secret 列表为空，旧 `CONFIG_ENCRYPTION_KEY_V1` 已删除，系统继续保持未初始化且新 `init_key` 鉴权成功。
- ADR `0050` 已接受：公开 GitHub 仓库是由完整开发仓库白名单生成的部署分发仓库，只包含 Vue、Worker、正式 D1 迁移、R2/KV 配置、部署工具、锁文件和简体中文部署说明；测试、验证原型、研发与验收记录、项目级 Skill、本地资源配置、密钥、日志、依赖和构建产物不进入公开仓库。公开目录必须在全新安装后通过 R2/KV 双模式检查才能推送。
- 第五十二批公开部署仓库已经完成：`https://github.com/wonly211/Simlettra` 为 Public，默认分支为 `main`，首个提交为 `127236c25470dbcf9d5f707f4d00ffeb0998a32d`，当前部署提交为 `ded2f43dddfbaee522edf8b232435482a381f541`，远程树包含 199 个部署文件且禁止路径为零。精简目录已通过全新依赖安装、冻结锁文件复核、R2/KV 类型检查、生产安全构建、绑定检查、自动资源命名与 D1 保留表模拟、初始化安全错误日志改动、敏感信息扫描和 Git 空白检查。两次真实 Cloudflare 构建已经依次发现默认配置和新 D1 保留表识别缺陷，当前修复后的重新部署仍待确认；当前不创建正式版本标签或 Release。
- `文档/验收/首发本地验收证据矩阵.md` 是当前验收证据索引；本地证据不能替代真实 Cloudflare Free、真实外部服务、跨浏览器、规模、安全总检查或非程序员真人验收。只要仍有“部分通过”或“未执行”，不得声称首发已经完成。
- 第六批数据-99 至数据-132 已全部接受：删除恢复、逻辑配额、Cloudflare 免费资源快照与保守停止、域名月度发件配额、审计、备份恢复、导出和迁移规则均已确认。ADR `0031` 至 `0033` 为“已接受”。
- 第六批具体设计、迁移草案和可重复验证原型已经建立。草案新增 31 张表，六批顺序执行共 94 张表；本地 SQLite 的 71 项删除、配额、资源快照、审计、备份、恢复、导出和迁移断言全部通过且外键检查为零。
- 免费资源范围已经收口：ADR `0029` 以 Workers Free 作为首发核心运行基线，ADR `0030` 以真实 Free 环境验证作为 20 MB 邮件解析上线门槛；旧 ADR `0006`、`0008` 的其余通用结论继续有效。
- 当前没有明确缺失的首发产品功能。下一阶段转入真实环境与发布验收：Cloudflare D1、Workers Free 20 MB MIME 解析、全域收信与认领、自动刷新请求与 D1 读取成本、Cron 心跳、KV/R2 多地区、大导出、Queue 并发、Resend 与 SMTP2GO 账号、五种通知通道、资源指标接口、备份恢复规模、升级回退、真实旧数据副本迁移和非程序员真人验收仍需后续远程复核。
- 第一批和第二批界面设计已于 2026-08-14 全部按推荐接受；同日项目所有者授权设计负责人独立完成整个 UI 设计，不再逐批请求确认，但不得自行改变产品功能、权限、数据或安全规则。完整界面定位为“澄明工作台”，桌面采用全高工作区，手机采用单栏分层；邮件、写信、设置、管理、登录、初始化、响应式和可访问性设计均已在当前前端实现，设计与验收文档位于 `文档/界面设计/`。
- 2026-08-14 已完成 `1440 × 900`、`1280 × 800`、`390 × 844` 和 `320 × 568` 界面复核；邮件阅读、会话、回复草稿、设置和运行健康页面无横向溢出，控制台没有页面警告或错误。手机邮件详情实现顶部复位、主题焦点和返回原邮件焦点，邮件详情、草稿编辑和设置建立轻量浏览历史。Vue 类型、前端 Lint、`1815` 个模板元素与 `371` 个原生控件可访问性自检、`56` 个测试文件的 `235` 项测试和生产安全构建均已通过；真实四浏览器、iOS VoiceOver、Android TalkBack、`200%` 放大和新增浏览器返回路径仍需外部复核。
