# 第三十六批：非程序员部署与初始化文档

## 状态

- 状态：本地文档完成，真实非程序员执行待验收
- 完成日期：2026-08-13
- 工程变化：新增部署与排错文档并统一远程手册命令；无产品代码、依赖、配置结构或 D1 迁移变化

> 后续取代：需求变更 `0011` 与 ADR `0049` 已在 2026 年 8 月 14 日把 Cloudflare 创建 Worker 并连接 GitHub 设为默认部署入口，并改用管理员可查看的 `INIT_KEY`、`CONFIG_KEY` 文本变量。本记录以下内容保留 2026 年 8 月 13 日当时以本机 Wrangler 和 Secret 为主流程的历史事实。

## 完成内容

1. 新增[首次部署与初始化指南](../部署/首次部署与初始化指南.md)，从准备清单、R2/KV 选择、Cloudflare 登录、资源创建、配置、迁移和部署写到双密钥、网页初始化、Email Routing 与首次自检。
2. 新增[基本排错指南](../运维/基本排错指南.md)，按安装、资源、迁移、部署、初始化、收信、发信、通知、存储和 Cron 的可观察现象组织排查路径。
3. 两份文档明确密钥、密码、令牌、正文和附件的记录禁区，并提供停止条件及脱敏问题记录模板。
4. 远程运行手册统一使用项目内锁定的 `pnpm exec wrangler`，避免依赖未知的全局 Wrangler；受保护密钥改为 Worker 首次部署后立即写入，再进入网页初始化。
5. 首发验收矩阵为验收-28补充文档证据，但仍保持“未执行”，没有把文档存在误写成真人流程已经通过。

## 本地核对范围

- Node.js、pnpm 和 Wrangler 版本取自当前正式工程配置。
- D1、R2、KV、Queue、迁移和密钥命令使用 Wrangler `4.120.1` 本地帮助核对。
- 初始化字段、地址字符、密码、时区、重复初始化和限速规则直接与当前服务端及网页实现核对。
- Cloudflare Vite 构建后配置和 Email Routing 步骤与 2026-08-13 的 Cloudflare 官方文档核对。
- R2/KV 双模式部署预检证据沿用第三十五批结果，没有连接或修改用户远程资源。
- 7 份本批相关当前文档共检查 42 个内部链接，没有失效链接或 UTF-8 BOM；首发清单与证据矩阵均保持 28 个唯一验收编号。
- 部署与远程手册没有发现绕过 `pnpm exec wrangler` 的资源创建或密钥命令。

## 仍未证明的内容

- 尚未由一名没有开发背景的真实用户在全新电脑和 Cloudflare Free 账号中独立执行。
- 尚未验证不同 Windows 环境中的 Node.js 与 pnpm 安装体验、Cloudflare 控制台实际文字变化和浏览器授权阻碍。
- 尚未完成从空白资源到多人共享收发邮件的真人验收，因此验收-28仍是“未执行”。

## 下一步

第三十七批整理首发发布资料与剩余门槛，建立版本行为、配置、数据影响、已知限制和发布阻断项的单一入口；真实凭据或远程资源仍不会被自动创建或修改。
