# 第四十批本地浏览器视觉补验

## 文档状态

- 状态：本地浏览器补验完成，原生后台与离线生命周期仍待补
- 完成日期：2026-08-14
- 对应验收：验收-08、验收-09、验收-23、验收-24、验收-25
- 工程变化：无运行代码、依赖或 D1 迁移变化

## 目标

补齐此前因应用内浏览器无法接管本地页面而遗留的永久删除、未分配地址认领、自动更新和邮件导出视觉证据，同时保留当前浏览器无法真实模拟的后台与离线边界。

## 验证结果

### 永久删除

- 桌面端打开个人邮件永久删除确认，影响说明完整，未勾选时最终按钮保持禁用，取消后邮件不受影响。
- `390 × 844` 手机端勾选后完成永久删除，明确提示同一物理邮件仍由已发送条目引用，因此正文和附件继续保留。
- 垃圾箱归零，页面无横向溢出，控制台没有警告或错误。

### 未分配地址认领

- 管理员通过网页把测试域名切换为全域收信并授权当前用户，再经本地 Worker Email Routing 入口向未知地址实际投递一封邮件。
- 桌面端未分配列表、详情、实际投递地址、认领影响说明和未勾选禁用态正确。
- 手机端确认后未分配列表归零，`visual-claim-0814@xn--fsqu00a.xn--fiqs8s` 成为个人别名，历史邮件进入个人收件箱并显示实际投递地址。
- 验收后域名已恢复“拒收未创建地址”，查看授权已撤销；开放未分配时期为零，外键检查为零。

### 网页新邮件自动更新

- 页面保持可见且不执行手动刷新时，两封经本地 Email Routing 投递的新邮件分别在 40 秒和 30 秒后出现在当前收件箱，符合每 60 秒检查的产品规则。
- 第二次自动更新发生时，正在阅读的邮件保持打开，列表从 4 封增至 5 封，没有清空当前阅读状态，也没有控制台错误。
- 当前浏览器控制面隐藏浏览器窗口后，网页的 `document.visibilityState` 仍是 `visible`，也不能真正切断网络；因此后台暂停、离线暂停、恢复在线和登录/登出生命周期不写成浏览器已通过，继续由自动化覆盖并保留真实浏览器补验。

### 个人与组织邮件导出

- 个人导出冻结 7 封邮件并生成 1 个可下载 ZIP 分卷。
- 新建测试组织并经本地 Email Routing 投递 1 封组织邮件后，组织创建者生成 1 个可下载 ZIP 分卷。
- 桌面和 `390 × 844` 手机页面均能选择范围、查看等待与成功状态、取得下载地址并删除导出记录；手机导出区域宽 335 px，页面无横向溢出，控制台没有警告或错误。
- 两次导出运行和制品均保留“已删除”事实，不再出现在当前列表。测试组织保留供后续共享验收复用。

## 结论

永久删除视觉缺口已关闭，验收-09 可以从“部分通过”更新为“本地通过”。未分配认领和邮件导出的本地视觉缺口已关闭，但真实 Cloudflare Free 并发、资源和大数据量门槛仍在。自动更新新增可见页面真实轮询证据，原生后台和离线生命周期仍未关闭。

相关记录：[第十三批邮件永久删除](第十三批邮件永久删除.md)、[第二十三批用户与组织邮件导出](第二十三批用户与组织邮件导出.md)、[第二十七批收信控制与全域认领](第二十七批收信控制与全域认领.md)、[第二十八批网页新邮件自动更新](第二十八批网页新邮件自动更新.md)。
