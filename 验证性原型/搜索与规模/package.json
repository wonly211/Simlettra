{
  "name": "simlettra-search-scale-prototype",
  "version": "0.0.0",
  "private": true,
  "description": "澄笺中文搜索与 D1 规模验证性原型",
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "validate": "node scripts/run-validation.mjs bigram",
    "validate:compare": "node scripts/run-validation.mjs bigram && node scripts/run-validation.mjs unigram-bigram"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260809.1",
    "typescript": "7.0.2",
    "wrangler": "4.120.0"
  }
}
